@model IEnumerable<DatabaseLayer.SupplierTable>

@{
    ViewBag.Title = "Finalize Purchasing";
}

<script src="~/Scripts/jquery-3.4.1.js"></script>

@using (Html.BeginForm("PurchaseConfirm", "Purchase", FormMethod.Post, null))
{
    @*@Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Purchase Details</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <label class="control-label col-md-2">Purchase Description</label>
            <div class="col-md-12">
                <input type="text" class="form-control" id="item.Description" name="item.Description" placeholder="Enter Purchase Description" required />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" style="float:right;" value="Finalize Purchase" class="btn btn-success" />
                <input type="text" id="search" class="form-control col-md-3" style="float:right;" placeholder="search here" />
            </div>
        </div>
    </div>*@

    <div class="card">
        <div class="card card-flat">
            <div class="card-header">
                <!-- Card header elements -->
                <input type="submit" style="float:right;" value="Finalize Purchase" class="btn btn-success" />
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover" id="datatable">
                        <thead style="background-color:lightgray;">
                            <tr>
                                <th>Supplier</th>
                                <th>Contact No</th>
                                <th>Email Supplier</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.SupplierName)</td>
                                    <td>@Html.DisplayFor(modelItem => item.ContactNo)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                    <td><input type="checkbox" name="name @item.SupplierID" class="chb" /></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

} <!-- Closing the form using block -->

<script type="text/javascript">
    $(document).ready(function () {
        //$("#search").on("keyup", function () {
        //    var value = $(this).val().toLowerCase();
        //    $("#supplierlist tr").filter(function () {
        //        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
        //    });
        //});
        $(".chb").change(function (event) {
            $(".chb").prop('checked', false);
            $(this).prop('checked', true);
        });
    });
</script>
